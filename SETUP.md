# 서버 붙이기 체크리스트

모임 궁합의 실시간·동기화·내 모임 목록은 Supabase가 있어야 돈다.
**여기 적힌 건 전부 당신이 직접 해야 하는 것들이다** (약관 동의와 콘솔 클릭은 대신 못 함).
다 하고 `.env` 값 두 개만 채우면 나머지 코드는 이미 붙어 있다.

키가 없으면 앱은 서버 기능 없이 그냥 돌아간다. 기존 `?p=` 링크도 계속 열린다.

---

## 1. Supabase 프로젝트 (5분)

1. https://supabase.com 가입 → **New project**
2. 이름 아무거나, 리전은 **Northeast Asia (Seoul)**, DB 비밀번호는 저장해두기
3. 프로젝트가 뜨면 **SQL Editor** → `supabase/schema.sql` 내용 통째로 붙여넣고 **Run**
   - 테이블 3개, RLS 정책, 인원 상한 트리거, 실시간 등록이 한 번에 들어간다
   - 여러 번 실행해도 안전하다
4. **Project Settings → API** 에서 두 값 복사
   - `Project URL`
   - `anon public` 키

## 2. 카카오 로그인 (15분)

카카오 → Supabase → 앱 순서로 값이 흘러간다.

**카카오 개발자 콘솔** (https://developers.kakao.com)

1. **내 애플리케이션 → 애플리케이션 추가하기** (앱 이름 `오늘운`, 사업자명은 개인 이름)
2. **앱 설정 → 앱 키** 에서 `REST API 키` 복사
3. **제품 설정 → 카카오 로그인** → 활성화 **ON**
4. 같은 화면의 **Redirect URI** 에 아래를 등록
   ```
   https://<프로젝트ID>.supabase.co/auth/v1/callback
   ```
   `<프로젝트ID>`는 1-4에서 복사한 Project URL에 들어 있다
5. **제품 설정 → 카카오 로그인 → 보안** → `Client Secret` 생성하고 **활성화 상태 ON**, 코드 복사
6. **앱 설정 → 비즈니스 → 개인 개발자 비즈 앱 전환** (개인으로 등록)
   - 본인인증과 약관 동의만 하면 되고 사업자등록번호나 데브톡 심사는 필요 없다
   - 이걸 해야 아래 이메일 동의항목이 열린다
7. **제품 설정 → 카카오 로그인 → 동의항목** → 아래 셋을 모두 설정한다
   - `닉네임` 필수 동의
   - `프로필 사진` 이용 중 동의
   - `카카오계정(이메일)` 선택 동의
   - **셋 다 켜야 한다.** Supabase는 카카오에 `account_email profile_image profile_nickname`을
     항상 요청하고, 앱 코드의 `scopes` 옵션으로 뺄 수 없다(덧붙이기만 된다).
     하나라도 설정 안 돼 있으면 로그인 화면에서 `KOE205`로 막힌다
8. **앱 설정 → 플랫폼 → Web** 에 사이트 도메인 등록 (배포 주소, 로컬 테스트용으로 `http://localhost:5173` 도 같이)

**Supabase 대시보드**

9. **Authentication → Sign In / Providers → Kakao** 활성화
   - `REST API Key` ← 2-2에서 복사한 REST API 키
   - `Client Secret Code` ← 2-5에서 복사한 코드
   - **Allow users without an email** 켜기 (7번에서 이메일을 선택 동의로 뒀으므로, 동의 안 한 사용자도 가입돼야 한다)
10. **Authentication → URL Configuration**
   - `Site URL` ← 배포 주소 (로컬만 테스트할 거면 `http://localhost:5173`)
   - `Redirect URLs` 에 배포 주소와 `http://localhost:5173` 둘 다 추가

## 3. 키 넣기

```bash
cp .env.example .env
```

`.env` 를 열어 1-4에서 복사한 값 두 개를 채운다.

```
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...
```

`anon` 키는 브라우저에 노출되는 게 정상이다. 권한은 위 RLS 정책이 막는다.
`.env` 는 `.gitignore` 에 들어 있다. **DB 비밀번호와 `service_role` 키는 여기 절대 넣지 마라** — 그건 RLS를 통째로 우회한다.

```bash
npm run dev
```

파티 탭에 카카오 로그인 버튼이 뜨면 성공.

---

## 확인해볼 것

- [ ] 로그인 → 모임 만들기 → 링크 복사
- [ ] 시크릿 창에서 그 링크 열기 → **로그인 없이** 모임이 보이고 내 생일 입력이 된다
- [ ] 시크릿 창에서 멤버 추가 → **원래 창이 새로고침 없이 갱신된다** (실시간)
- [ ] 다른 기기에서 같은 카카오 계정 로그인 → 내 생일과 모임 목록이 따라온다

## 알아둘 것

- **참여한 모임도 내 목록에 뜬다.** 링크로 그냥 들어간 기록은 이 기기에 남아 있다가, 나중에 로그인하면 서버의 내 행에 이름표를 붙인다(`익명 참여는 나중에 본인이 가져간다` 정책). 그때부터 다른 기기에서도 보인다. 로그인 전에는 참여한 그 기기에서만 보인다.
- **비로그인 참여자는 자기가 넣은 걸 못 지운다.** 서버가 그 사람이 누군지 알 방법이 없어서다. 오타 나면 모임 만든 사람이 지워주면 된다. 이걸 풀려면 참여도 로그인 필수로 바꿔야 한다.
- **링크를 아는 사람은 누구나 본다.** id가 곧 열쇠다(랜덤 16자리). 단톡방 밖으로 새면 그 사람도 본다.
- **인원은 모임당 30명까지.** 참여가 로그인 없이 열려 있어서 둔 상한이다. `schema.sql` 의 `check_party_size` 에서 바꿀 수 있다.
- 무료 티어 프로젝트는 일주일 넘게 요청이 없으면 일시정지된다. 대시보드에서 되살리면 된다.
